I. Если нет на компе bower, gulp (если уже установлены, то к пункты II)
    a. npm install -g bower
    б. npm install --global gulp

II. Зайдя в папку проекта устанавливаем
    a. npm install
    б. bower install
    с. gulp


III. Если вы хотите добавить какие-то новые возможности по npm gulp
    1. npm install --save-dev /*Название плагина*/
    2. bower install --save /*Название поашина*/# basis_ 


 # basis_ 
